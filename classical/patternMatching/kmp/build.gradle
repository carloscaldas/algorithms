apply plugin: 'java'
apply plugin: 'eclipse'

version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
	testCompile group: 'junit', name: 'junit', version: '4.+'
}

task "prepare" << {
	//println sourceSets.java.srcDirs
   sourceSets*.java.srcDirs*.each { println(it) }
   sourceSets*.resources.srcDirs*.each { it.mkdirs() }
}

